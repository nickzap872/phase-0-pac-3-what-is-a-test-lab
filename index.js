const name = "Susan"; // Update name to "Susan"
const height = 30;    // Update height to a value less than 40
const message = `${name} is ${height} inches tall`;

module.exports = {name, height, message};
